# Live-demo data sources (optional — defaults to the vallov.com instance)
# Used by docs-site demos only; the published library never reads these.
VITE_EF_API_BASE=https://api.vallov.com
VITE_EF_CDN_BASE=https://cdn.vallov.com
